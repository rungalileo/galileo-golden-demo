# API Keys
<<<<<<< Updated upstream
openai_api_key = ""
galileo_api_key = ""
PHOENIX_API_KEY = "" #If using phoenix logging
ARIZE_API_KEY = ""  #If using Arize AX logging
LANGSMITH_API_KEY = "" #If using LangSmith logging
BRAINTRUST_API_KEY = "" #If using Braintrust logging
LANGFUSE_API_SK="" #langfuse secret key (If using Langfuse logging)
LANGFUSE_API_PK="" #langfuse public key (If using Langfuse logging)

# Galileo Configuration
galileo_project = ""
galileo_log_stream = ""
galileo_console_url = "https://app.galileo.ai"

# Used to authorize users for deleting experiments
admin_key = ""

# Phoenix (Arize AX) Configuration - Primary observability platform
PHOENIX_ENDPOINT="https://app.phoenix.arize.com/s/<org...>/v1/traces"
PHOENIX_PROJECT=""

# Arize Platform/AX Configuration
# NOTE: Phoenix and Arize AX both use OpenTelemetry and are mutually exclusive
# You can toggle between them in the UI (ARIZE_API_KEY defined above)
ARIZE_SPACE_ID=""
ARIZE_PROJECT=""

#Langsmith Configuration (uses callbacks, not OTLP)
LANGCHAIN_TRACING_V2="true"
LANGCHAIN_API_KEY="" # Note: same key as LangSmith above (Need to refactor)
LANGCHAIN_PROJECT=""


#Langfuse Configuration
LANGFUSE_HOST="https://us.cloud.langfuse.com"
LANGFUSE_PROJECT=""

#Braintrust Configuration
BRAINTRUST_PROJECT=""


APHLA_VANTAGE_API_KEY="" #Find our paid one in 1Password
FINNHUB_API_KEY="" #Generate a free one for private use
NEWSAPI_KEY="" #Generate a free one for private use

USE_LIVE_DATA="TRUE"
USE_CHAOS_TOOLS="TRUE"

# Pinecone Configuration
# Each project has its own API key, need both to create vector dbs for both local and hosted demos
pinecone_api_key_local = ""    # For galileo-demo-local project
pinecone_api_key_hosted = ""   # For galileo-demo-hosted project

# Environment Configuration
# Set to "local" for local development, "hosted" for production
environment = "local"
=======
# -----------------------------------------------------------------------------
# OpenAI (required)

openai_api_key = "..."
openai_default_chat_model = "gpt-4o"
openai_embedding_model = "text-embedding-3-large"

galileo_api_key = "..."

# Galileo Configuration
# -----------------------------------------------------------------------------
# Each domain automatically gets its own Galileo project:
#   Default: "galileo-demo-{domain_name}"
#   Example: finance domain → "galileo-demo-finance"
#
# To override the default project name for a domain, edit:
#   domains/{domain}/config.yaml
# -----------------------------------------------------------------------------
# Console URL for your Galileo instance

galileo_project = "galileo-demo"
galileo_log_stream = "demo"
galileo_console_url = "https://console...."

# Agent Control (runtime guardrails)
# Create controls manually in the Agent Control server for step names:
# Bank Assistant, tool_step, retrieval_step

galileo_api_url = "https://api...."
agent_control_url = "https://console..../api/agent-control"

agent_control_agent_name = "golden-demo-hosted"
agent_control_runtime_auth_mode="jwt"
agent_control_api_key_header = "Galileo-API-Key"
agent_control_target_type="log_stream"


# PostgreSQL Configuration (pgvector)
# -----------------------------------------------------------------------------
# PostgreSQL with pgvector extension for vector storage.
# See README for Docker setup instructions.
postgres_host="..."
postgres_port = "5432"
postgres_user = "..."
postgres_password = "..."
postgres_db = "..."

# Environment Configuration
# -----------------------------------------------------------------------------
# Set to "local" for local development, "hosted" for production/deployed demos
# This determines which pgvector collection prefix is used for vector storage.
environment = "local"

# Live Finance Data (Optional)
# -----------------------------------------------------------------------------
# Alpha Vantage API key for backup stock data source.
# Get a free key at: https://www.alphavantage.co/support/#api-key
# Note: Yahoo Finance (yfinance) is used as the primary source and requires no key.
alpha_vantage_api_key = "..."

# Admin Configuration
# -----------------------------------------------------------------------------
# Used to authorize users for deleting experiments in the UI
admin_key = ""
>>>>>>> Stashed changes
