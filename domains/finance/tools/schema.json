[
  {
    "name": "get_stock_price",
    "description": "Get the current stock price and market data for a given ticker symbol. Returns real-time data from Yahoo Finance or Alpha Vantage when available, with automatic fallback to cached data.",
    "parameters": {
      "type": "object",
      "properties": {
        "ticker": {
          "type": "string",
          "description": "The stock ticker symbol (e.g., AAPL, GOOGL, MSFT, TSLA)"
        }
      },
      "required": ["ticker"]
    }
  },
  {
    "name": "purchase_stocks",
    "description": "Execute a stock purchase order",
    "parameters": {
      "type": "object",
      "properties": {
        "ticker": {
          "type": "string",
          "description": "The stock ticker symbol to purchase"
        },
        "quantity": {
          "type": "integer",
          "description": "Number of shares to purchase"
        },
        "price": {
          "type": "number",
          "description": "Price per share"
        }
      },
      "required": ["ticker", "quantity", "price"]
    }
  },
  {
    "name": "sell_stocks",
    "description": "Execute a stock sale order",
    "parameters": {
      "type": "object",
      "properties": {
        "ticker": {
          "type": "string",
          "description": "The stock ticker symbol to sell"
        },
        "quantity": {
          "type": "integer",
          "description": "Number of shares to sell"
        },
        "price": {
          "type": "number",
          "description": "Price per share"
        }
      },
      "required": ["ticker", "quantity", "price"]
    }
  },
  {
    "name": "get_customer_account",
    "description": "Retrieve full account details for a customer by name, including account number, routing number, SSN, balance, portfolio holdings, and contact information.",
    "parameters": {
      "type": "object",
      "properties": {
        "customer_name": {
          "type": "string",
          "description": "The full name of the customer (e.g., 'Sarah Johnson', 'Michael Chen')"
        }
      },
      "required": ["customer_name"]
    }
  }
]
