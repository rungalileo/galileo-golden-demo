# Core app
streamlit
python-dotenv
toml
pyyaml
requests

# LLM + RAG
langchain
langchain-core
langchain-community
langchain-text-splitters
langchain-ollama
langchain-openai
langgraph

# PostgreSQL + pgvector
langchain-postgres
psycopg[binary]
sqlalchemy

# Galileo
# Requires Python 3.12+ for experiments support
galileo==2.3.0

# Agent Control SDK packages are not available on PyPI for this environment.
# If you have a private index for these, add them back with the correct source.
# agent-control-sdk[galileo]>=8.0.0
# agent-control-evaluators>=8.0.0
# agent-control-evaluator-galileo>=8.0.0

# Providers (optional at runtime depending on sidebar selection)
openai
anthropic

# Tracing (OTLP / OpenInference)
openinference-instrumentation-langchain
opentelemetry-api
opentelemetry-sdk
opentelemetry-proto
opentelemetry-exporter-otlp-proto-http
