extensions:
  health_check:
    endpoint: 0.0.0.0:13133

receivers:
  otlp:
    protocols:
      grpc:
        endpoint: 0.0.0.0:4317
      http:
        endpoint: 0.0.0.0:4318

processors:
  batch:
    timeout: 5s
    send_batch_size: 512
  resourcedetection:
    detectors: [system]
    timeout: 10s

exporters:
  sapm:
    access_token: sVj15AgwXYTVaPE8Ml5eFA
    endpoint: https://ingest.us1.signalfx.com/v2/trace
  signalfx:
    access_token: sVj15AgwXYTVaPE8Ml5eFA
    realm: us1

service:
  extensions: [health_check]
  pipelines:
    traces:
      receivers: [otlp]
      processors: [resourcedetection, batch]
      exporters: [sapm]
    metrics:
      receivers: [otlp]
      processors: [resourcedetection, batch]
      exporters: [signalfx]
