[pytest]
markers =
    integration: end-to-end tests that hit real LLM / Galileo / Pinecone APIs (skipped by default)

# Skip integration tests unless explicitly requested with `-m integration`.
addopts = -m "not integration"
